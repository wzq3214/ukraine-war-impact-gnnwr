# Validation status

The repository utility layer has been checked by Python compilation, the built-in self-test and an audit-only run on the analytical workbook supplied during code preparation. The audit confirmed the required fields, unique ADM3 identifiers, the use of `CII_pos`, and low collinearity among the four model covariates.

A full GNNWR training run must be completed in the target computing environment with the final analytical workbook and `UKR_ADM3_2023` boundary before manuscript values are frozen. Table 3 and Figure 6 must be taken from that formal run rather than copied from hard-coded values.
